import { View, Text } from "@tarojs/components";
import "./index.css";

export default function Notes() {
  return (
    <View className="page">
      <Text>备注页面（备用入口，主要在订单详情内使用）</Text>
    </View>
  );
}
